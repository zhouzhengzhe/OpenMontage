interface:
  display_name: "OpenMontage"
  short_description: "按需调用中央 OpenMontage 视频制作环境"
  default_prompt: "Use $openmontage to run an OpenMontage video-production workflow."

policy:
  allow_implicit_invocation: false
